# Angular's ecosystem often has strict peer ranges that lag/lead the core version.
# legacy-peer-deps lets npm install when a transitive peer (e.g. a chart lib) is a
# minor version off, instead of aborting with ERESOLVE.
legacy-peer-deps=true
